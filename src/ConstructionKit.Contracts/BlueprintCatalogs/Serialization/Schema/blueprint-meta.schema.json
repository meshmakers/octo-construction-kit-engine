{
  "$id": "https://schemas.meshmakers.cloud/blueprint-meta.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Blueprint Metadata",
  "description": "Schema for blueprint metadata files",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string",
      "format": "uri",
      "description": "The URI of the schema for the blueprint meta"
    },
    "blueprintId": {
      "type": "string",
      "pattern": "^[a-zA-Z0-9.-]+-[0-9]+\\.[0-9]+\\.[0-9]+$",
      "description": "The unique identifier of the blueprint in format 'Name-Major.Minor.Patch'. Names may contain dots; the OctoMesh convention is that names starting with 'System.' are service-managed (e.g. System.Communication-1.0.0)."
    },
    "description": {
      "type": "string",
      "description": "An optional description of the blueprint"
    },
    "ckModelDependencies": {
      "type": "array",
      "description": "List of CK model dependencies required by this blueprint",
      "items": {
        "type": "string",
        "pattern": "^[a-zA-Z0-9.-]+-\\[[0-9]+\\.[0-9]+(\\.[0-9]+)?,([0-9]+\\.[0-9]+(\\.[0-9]+)?)?\\)$",
        "description": "CK model dependency in format 'Name-[MinVersion,MaxVersion)' (Name may contain dots, e.g. System.Communication)."
      }
    },
    "blueprintDependencies": {
      "type": "array",
      "description": "Blueprints that must be installed before this one. Resolved transitively and topo-sorted at install time.",
      "items": {
        "type": "string",
        "pattern": "^[a-zA-Z0-9.-]+-\\[[0-9]+\\.[0-9]+(\\.[0-9]+)?,([0-9]+\\.[0-9]+(\\.[0-9]+)?)?\\)$",
        "description": "Blueprint dependency in format 'Name-[MinVersion,MaxVersion)' (Name may contain dots)."
      }
    },
    "seedDataPath": {
      "type": "string",
      "description": "Path to the seed data file relative to the blueprint root directory"
    },
    "requires": {
      "type": "object",
      "description": "Optional preconditions evaluated before a blueprint is applied. Each key is the name of a blueprint variable (e.g. octo.environment, octo.isSystemTenant) and the value is the expected value, or a list of acceptable values. The blueprint is skipped (success, no install row, no seed) when the resolved variable is missing from the tenant context or does not match any of the listed values. Requires are only evaluated on the root blueprint of an apply call — transitive dependencies are always installed.",
      "additionalProperties": {
        "oneOf": [
          { "type": "string" },
          { "type": "array", "items": { "type": "string" } }
        ]
      }
    },
    "migrations": {
      "type": "array",
      "description": "Migration scripts from previous versions",
      "items": {
        "type": "object",
        "properties": {
          "fromVersion": {
            "type": "string",
            "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$",
            "description": "The source version this migration applies from"
          },
          "scriptPath": {
            "type": "string",
            "description": "Path to the migration script file (relative to blueprint root)"
          },
          "description": {
            "type": "string",
            "description": "Optional description of the migration"
          }
        },
        "required": ["fromVersion", "scriptPath"],
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false,
  "required": [
    "blueprintId"
  ]
}
