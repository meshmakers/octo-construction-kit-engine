"$schema": "https://schemas.meshmakers.cloud/construction-kit-meta.schema.json"
modelId: "System-2.1.0"
description: "Core system model library providing foundational types for entity management, multi-tenant configuration, auto-increment sequences, a comprehensive query system with support for filtering/sorting/aggregations/text-search, and the System/TimeRange record used by stream-data archives and any other System-level CK that needs windowed values."
