"$schema": "https://schemas.meshmakers.cloud/construction-kit-meta.schema.json"
modelId: "System-2.0.8"
description: "Core system model library providing foundational types for entity management, multi-tenant configuration, auto-increment sequences, and a comprehensive query system with support for filtering, sorting, aggregations, and text search."
